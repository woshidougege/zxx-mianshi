// 技术术语词典 - 包含所有需要解释的术语
const TERMS_DICTIONARY = {
    // 测试工具
    'pytest': {
        pronunciation: '派测特',
        explain: `pytest，Python的测试框架，支持丰富的断言和插件。

✦ 核心特点：
• 简洁：测试用例编写简单，无需继承特定类
• 灵活：支持多种测试风格（单元、集成、端到端）
• 插件化：丰富的插件生态，如allure报告、pytest-html
• 自动发现：自动发现测试文件和测试函数

✦ 测试场景：
• 接口自动化：结合requests库进行接口测试
• 单元测试：对Python函数或类进行测试
• Web UI自动化：结合Selenium/Playwright进行UI测试

✦ 面试话术：
• "我主要用pytest来做接口自动化测试，因为它语法简洁，上手快，而且有丰富的插件生态，比如allure可以生成漂亮的测试报告。我通常会结合requests库来发送HTTP请求，然后用pytest的断言来验证响应。"`
    },
    
    'JMeter': {
        pronunciation: '杰米特',
        explain: `Apache JMeter，开源性能测试工具，功能强大。

✦ 核心功能：
• 性能测试：压力测试、负载测试、并发测试
• 接口测试：支持HTTP、HTTPS、SOAP等协议
• 数据库测试：支持JDBC连接测试
• 多协议支持：FTP、LDAP、TCP等

✦ 关键组件：
• 线程组：模拟用户并发
• 采样器：发送请求（HTTP Request）
• 监听器：查看结果（聚合报告、图形结果）
• 断言：验证响应结果
• 定时器：控制请求间隔

✦ 性能指标：
• QPS（每秒请求数）
• 响应时间（RT）
• 错误率
• 吞吐量

✦ 面试话术：
• "我用JMeter做过性能测试，主要是配置线程组模拟并发用户，设置HTTP请求采样器，然后通过聚合报告查看QPS、响应时间等指标。压测时会逐步增加并发数，观察系统性能曲线，找到系统瓶颈。"`
    },
    
    'Postman': {
        pronunciation: '波斯特曼',
        explain: `Postman，最流行的接口测试工具，图形化操作，简单易用。

✦ 核心功能：
• 接口调试：发送HTTP请求，查看响应
• 用例管理：组织接口到集合（Collection）
• 环境变量：不同环境（测试/生产）快速切换
• 脚本功能：Pre-request Script、Tests断言
• 自动化：Newman命令行工具批量执行

✦ 常用场景：
• 手工接口测试：调试单个接口
• 接口联调：前后端联调验证
• 快速验证：验证bug是否修复
• 导出分享：导出Collection分享给团队

✦ 面试话术：
• "Postman是我日常用得最多的接口测试工具，主要用来快速验证接口是否正常。我会把接口组织到Collection里，配置环境变量来区分测试环境和线上环境。对于需要重复执行的接口，我会写成自动化用例。"`
    },
    
    'Jenkins': {
        pronunciation: '詹金斯',
        explain: `Jenkins，开源CI/CD工具，自动化测试的核心平台。

✦ 核心功能：
• 持续集成：代码提交后自动构建测试
• 定时任务：定时执行自动化脚本
• 流水线：Pipeline实现复杂构建流程
• 插件丰富：Git、Docker、allure等

✦ 典型流程：
1. 拉取代码（Git）
2. 安装依赖（pip install）
3. 执行测试（pytest）
4. 生成报告（allure）
5. 发送邮件通知

✦ 面试话术：
• "我们项目用Jenkins做自动化回归，配置定时任务每天凌晨跑一次。Pipeline会先从Git拉代码，然后执行pytest脚本，最后生成allure报告。如果有失败用例，Jenkins会发邮件通知。"`
    },
    
    'Fiddler': {
        pronunciation: '费德勒',
        explain: `Fiddler，Windows平台流行的抓包工具。

✦ 核心功能：
• HTTP/HTTPS抓包
• 请求重放（Replay）
• 断点调试（Breakpoint）
• 弱网模拟
• 修改请求/响应

✦ 使用场景：
• 接口调试：查看请求参数和响应
• 问题定位：分析请求失败原因
• 安全测试：修改参数测试越权
• 性能分析：查看接口响应时间

✦ 面试话术：
• "我常用Fiddler抓包分析接口问题，可以清楚看到请求的Headers、Body和响应内容。有时会用断点功能拦截请求，修改参数后重新发送，验证异常场景。"`
    },
    
    'Charles': {
        pronunciation: '查尔斯',
        explain: `Charles，跨平台抓包工具，Mac/Win都支持。

✦ 核心功能：
• HTTP/HTTPS抓包
• 断点调试（Breakpoints）
• 弱网模拟（Throttle Settings）
• Map Local/Remote（本地/远程映射）
• SSL代理配置

✦ 与Fiddler对比：
• 界面更友好
• 跨平台（Mac首选）
• 树形结构清晰
• 弱网模拟更强

✦ 面试话术：
• "Charles是我在Mac上主要用的抓包工具，界面比Fiddler更直观。我经常用它的弱网模拟功能测试网络不好时的表现，还可以用Map Local功能做接口mock。"`
    },
    
    'Redis': {
        pronunciation: '瑞迪斯',
        explain: `Redis，开源内存数据库，常用作缓存。

✦ 核心特点：
• 速度快：内存存储，性能极高
• 数据结构：string、hash、list、set、zset
• 持久化：RDB快照、AOF日志
• 分布式：支持主从复制、集群

✦ 测试要点：
• 缓存一致性：Redis与MySQL数据是否一致
• 缓存穿透：查询不存在的数据
• 缓存雪崩：大量key同时失效
• 缓存击穿：热点key失效
• 降级策略：Redis挂了怎么办

✦ 面试话术：
• "在测试中，我会重点关注Redis缓存一致性。比如更新商品信息后，要验证Redis缓存是否正确更新。还会测试Redis挂掉后的降级策略，看系统是否能正常降级到数据库查询。"`
    },
    
    'MySQL': {
        pronunciation: '买埃斯Q L',
        explain: `MySQL，最流行的关系型数据库。

✦ 测试中的应用：
• 数据验证：验证接口操作后数据库数据是否正确
• 造数据：通过SQL插入测试数据
• 数据清理：测试后清理脏数据
• 性能分析：查看慢查询日志

✦ 常用SQL：
• 查询：SELECT * FROM user WHERE id=1
• 插入：INSERT INTO user VALUES (...)
• 更新：UPDATE user SET name='test'
• 删除：DELETE FROM user WHERE id=1
• 关联：JOIN、LEFT JOIN查询

✦ 面试话术：
• "测试中我经常用SQL验证数据正确性，比如测试下单接口后，会查询订单表验证订单状态、金额等字段是否正确。有时也会直接用SQL构造测试数据，提高测试效率。"`
    },
    
    'Python': {
        pronunciation: '派森',
        explain: `Python，简单易学的编程语言，测试首选。

✦ 为什么测试选Python：
• 语法简单：比Java、C++容易学
• 库丰富：requests、pytest、selenium等
• 社区活跃：问题容易找到答案
• 效率高：快速编写测试脚本

✦ 测试常用库：
• requests：接口测试
• pytest：测试框架
• selenium：UI自动化
• allure：测试报告
• paramiko：SSH连接

✦ 基础语法：
• 变量：name = "test"
• 列表：[1, 2, 3]
• 字典：{'name': 'test'}
• 循环：for i in range(10)
• 函数：def test_login()

✦ 面试话术：
• "我用Python写接口自动化脚本，主要用requests发送HTTP请求，用pytest组织用例，用allure生成报告。Python语法简单，写脚本效率很高。"`
    },
    
    'requests': {
        pronunciation: '瑞亏斯特',
        explain: `requests，Python最流行的HTTP库，接口测试必备。

✦ 核心功能：
• 发送请求：GET、POST、PUT、DELETE
• 请求参数：params、data、json、headers
• 响应处理：status_code、text、json()
• Session管理：保持登录态
• 超时设置：timeout参数
• 异常处理：try-except捕获

✦ 示例代码：
\`\`\`python
import requests

# GET请求
response = requests.get('https://api.example.com/user/1')
print(response.json())

# POST请求
data = {'username': 'test', 'password': '123456'}
response = requests.post('https://api.example.com/login', json=data)
\`\`\`

✦ 结合pytest使用：
\`\`\`python
def test_get_user():
    response = requests.get('https://api.example.com/user/1')
    assert response.status_code == 200
    assert response.json()['name'] == 'test'
\`\`\`

✦ 面试话术：
• "我用requests库做接口自动化，它封装了HTTP请求，使用很简单。比如发送GET请求就是requests.get()，响应用.json()直接转成字典，然后用assert断言验证。"`
    },
    
    'Selenium': {
        pronunciation: '瑟利尼姆',
        explain: `Selenium，Web UI自动化测试框架。

✦ 核心功能：
• 元素定位：id、class、xpath、css
• 操作元素：click()、send_keys()、clear()
• 等待机制：隐式等待、显式等待
• 多浏览器：Chrome、Firefox、Safari
• 截图：screenshot()

✦ 元素定位方法：
• ID：driver.find_element_by_id("username")
• Class：driver.find_element_by_class_name("btn")
• XPath：driver.find_element_by_xpath("//input[@id='username']")
• CSS：driver.find_element_by_css_selector("#username")

✦ 面试话术：
• "我用Selenium做过一些UI自动化，主要是自动化主流程回归。通过定位元素，模拟用户操作，验证页面展示。不过UI自动化维护成本高，我更倾向于接口自动化。"`
    },
    
    'allure': {
        pronunciation: '阿乐',
        explain: `allure，最流行的测试报告框架，让测试报告更专业。

✦ 核心特点：
• 美观：图表丰富，数据直观
• 详细：步骤、截图、日志清晰
• 趋势：历史趋势对比
• 分类：按功能模块、优先级分类

✦ 使用方法：
\`\`\`python
# 安装
pip install allure-pytest

# 执行测试生成数据
pytest --alluredir=./allure-results

# 生成HTML报告
allure generate ./allure-results -o ./allure-report
\`\`\`

✦ 面试话术：
• "我们自动化测试用allure生成报告，它比pytest自带的报告美观很多，有饼图、趋势图、失败截图，领导看起来很直观。"`
    },
    
    'Git': {
        pronunciation: '吉特',
        explain: `Git，分布式版本控制系统，代码管理必备。

✦ 核心概念：
• 工作区：本地修改代码的地方
• 暂存区：git add后的临时存储
• 仓库：git commit后的永久存储
• 分支：并行开发不同功能

✦ 常用命令：
• git clone：克隆仓库
• git pull：拉取最新代码
• git add：添加到暂存区
• git commit：提交到仓库
• git push：推送到远程
• git branch：分支管理

✦ 面试话术：
• "我们项目用Git做版本管理，我会把自动化脚本提交到Git仓库，方便团队协作。平时用git pull拉取最新代码，修改后git add、git commit、git push提交。"`
    },
    
    'kones': {
        pronunciation: '可你斯',
        explain: `kones，贝壳内部的CI/CD平台。

✦ 核心功能：
• 流水线管理：可视化Pipeline配置
• 自动化准入：代码提交自动触发测试
• 质量门禁：测试不通过禁止发布
• 多环境支持：测试、预发、生产
• 权限管理：不同角色不同权限

✦ 与Jenkins对比：
• 更贴合贝壳业务
• 界面更现代化
• 集成贝壳内部工具
• 权限管理更细致

✦ 面试话术：
• "在贝壳我们用kones做CI/CD，它是贝壳内部开发的平台，比Jenkins更贴合我们的业务。我配置流水线，设置自动化准入，测试用例不通过就不允许发布，保证了代码质量。"`
    },
    
    'Apollo': {
        pronunciation: '阿波罗',
        explain: `Apollo，携程开源的配置中心，集中管理配置。

✦ 核心功能：
• 配置管理：集中管理所有环境配置
• 实时生效：配置修改实时生效，无需重启
• 版本管理：配置历史版本可回滚
• 权限控制：不同角色不同权限
• 多环境：开发、测试、生产隔离

✦ 测试要点：
• 配置修改：验证修改后是否生效
• 配置回滚：验证回滚功能
• 权限校验：非管理员不能修改
• 多环境：不同环境配置隔离

✦ 配置示例：
\`\`\`
# 数据库配置
db.url=jdbc:mysql://localhost:3306/test
db.username=root
db.password=123456

# 功能开关
feature.newUI.enabled=true
feature.payment.mock=false
\`\`\`

✦ 面试话术：
• "我们项目用Apollo做配置管理，测试时会验证配置修改是否实时生效。比如改个功能开关，看页面是否立即变化，不需要重启服务。"`
    },
    
    // ===== AI工具相关 =====
    'Cursor': {
        pronunciation: '克瑟',
        explain: `Cursor，AI编程助手，VSCode的升级版，内置GPT-4。

✦ 核心功能：
• AI补全：智能代码补全，比传统IDE更强
• Chat对话：直接在编辑器里问AI问题
• Cmd+K：选中代码，让AI帮你修改
• 上下文理解：AI能看到整个项目代码

✦ 测试场景应用：
• 编写测试用例：选中函数，让AI生成pytest用例
• 编写自动化脚本：描述需求，AI生成完整脚本
• Bug调试：贴错误日志，AI分析原因
• 代码重构：让AI优化代码结构

✦ 与传统IDE对比：
• VSCode：手工写代码
• Cursor：AI辅助写代码，效率提升50%+

✦ 面试话术：
• "我用Cursor写测试脚本，效率提升了很多。比如写接口自动化，我只需要描述需求，Cursor就能生成基础的pytest代码框架，我再根据实际情况调整。特别是写正则表达式、复杂的SQL查询，Cursor都能快速生成。"`
    },
    
    'GitHub Copilot': {
        pronunciation: 'GitHub 考派勒特',
        explain: `GitHub Copilot，GitHub推出的AI编程助手。

✦ 核心功能：
• 代码补全：根据注释和上下文生成代码
• 多语言支持：Python、Java、JS等全支持
• 函数生成：写注释，自动生成函数体
• 测试生成：写函数，自动建议测试用例

✦ 如何使用：
1. 安装Copilot插件（VSCode/JetBrains）
2. 写注释：# 发送GET请求获取用户信息
3. AI自动补全代码
4. Tab键接受，继续写

✦ 测试场景：
• 接口测试：注释"测试登录接口"，自动生成requests代码
• 数据构造：注释"生成100个测试用户"，自动生成循环代码
• 断言：写"验证响应状态码"，自动生成assert

✦ 面试话术：
• "我用GitHub Copilot辅助写自动化脚本，它能根据注释生成代码，特别适合写一些重复性的代码。比如构造测试数据、写相似的测试用例，效率提升明显。"`
    },
    
    'ChatGPT': {
        pronunciation: '恰特GPT',
        explain: `ChatGPT，OpenAI的对话式AI，最流行的AI工具。

✦ 测试场景应用：
• 用例设计：描述功能，AI帮你设计测试用例
• 边界值分析：让AI分析边界场景
• 测试策略：问AI怎么测试某个复杂功能
• Bug分析：贴日志，AI帮分析问题原因
• 脚本编写：描述需求，AI生成代码

✦ 实用Prompt模板：
\`\`\`
我要测试一个登录功能，用户名6-20位，密码8-16位，
请帮我设计测试用例，包括正常、异常、边界场景
\`\`\`

✦ 优缺点：
• 优点：理解能力强，回答详细
• 缺点：国内需要魔法，有时会"胡说八道"

✦ 面试话术：
• "我用ChatGPT辅助测试工作，比如设计测试用例，我会把需求描述给它，让它帮我梳理测试点。还用来分析日志，把报错贴给它，它能快速定位可能的原因。"`
    },
    
    'Claude': {
        pronunciation: '克劳德',
        explain: `Claude，Anthropic公司的AI助手，ChatGPT的竞品。

✦ 特点：
• 更严谨：回答更准确，较少胡说
• 长文本：支持10万token上下文（ChatGPT只有8k）
• 代码能力：写代码质量高
• 分析能力：适合分析复杂日志和文档

✦ 测试场景：
• 分析长日志文件：可以把整个日志文件贴进去分析
• 复杂业务理解：分析复杂的需求文档
• 代码审查：贴测试脚本，让它审查优化
• 报告生成：根据测试数据生成测试报告

✦ 与ChatGPT对比：
• Claude：更适合分析、理解、写代码
• ChatGPT：更适合创意、对话、头脑风暴

✦ 面试话术：
• "我比较喜欢用Claude分析问题，比如线上出了bug，日志很长，我会贴给Claude让它帮忙定位问题。Claude的回答比较严谨，较少出错。"`
    },
    
    '千问': {
        pronunciation: '千问',
        explain: `千问（Qianwen），阿里云推出的国产大模型。

✦ 特点：
• 国产：无需魔法，国内访问快
• 免费：基础版免费使用
• 中文强：中文理解能力强
• 联网：可以搜索最新信息

✦ 测试场景：
• 中文需求理解：分析中文需求文档
• 测试用例设计：生成中文测试用例
• 问题分析：分析测试问题
• 脚本生成：生成测试脚本

✦ 面试话术：
• "我也用千问这些国产AI工具，不用翻墙，访问快，中文理解能力也不错。日常工作中用来快速查一些技术问题，或者让它帮忙写一些简单的测试脚本。"`
    },
    
    'DeepSeek': {
        pronunciation: 'Deep Seek',
        explain: `DeepSeek，国产大模型，代码能力强，免费。

✦ 核心特点：
• 代码能力强：写代码质量高，特别是Python
• 完全免费：API调用免费
• 速度快：响应速度快
• 开源：可以本地部署

✦ 为什么选DeepSeek：
• 写代码质量好，适合测试脚本开发
• 免费，团队可以放心用
• 国内访问稳定

✦ 测试场景：
• 编写自动化脚本：生成pytest/selenium代码
• 代码优化：优化现有测试脚本
• Bug修复：分析代码问题
• 工具脚本：写造数据、数据处理脚本

✦ 面试话术：
• "最近DeepSeek很火，我试用了一下，代码能力确实强。我用它写过一些测试工具脚本，代码质量不错，而且免费，团队协作时大家都能用。"`
    },
    
    'Prompt': {
        pronunciation: 'Prompt',
        explain: `Prompt（提示词），与AI对话的"咒语"，决定AI回答质量。

✦ 什么是Prompt：
就是你给AI的指令和问题，好的Prompt能让AI给出更准确的答案

✦ Prompt工程技巧：
1. 明确角色：你是一个资深测试工程师
2. 清晰需求：我要测试登录功能，需要设计测试用例
3. 提供上下文：用户名6-20位，密码8-16位，支持记住密码
4. 指定格式：请用表格形式输出，包含用例编号、步骤、预期结果
5. 举例说明：比如"测试用例001：输入正确用户名密码，期望登录成功"

✦ 好的Prompt示例：
\`\`\`
你是一个资深测试工程师。
我需要测试一个登录接口，要求如下：
- URL: /api/login
- 方法: POST
- 参数: username(6-20位), password(8-16位)
- 返回: {code: 200, token: "xxx"}

请帮我设计接口测试用例，包括：
1. 正常场景
2. 异常场景（参数缺失、格式错误）
3. 边界场景（最大最小长度）
4. 安全场景（SQL注入、XSS）

请用表格格式输出，包含：用例编号、场景、请求参数、预期结果
\`\`\`

✦ 面试话术：
• "我了解一些Prompt工程的基础技巧，比如跟AI对话时，我会明确告诉它我的角色和需求，提供详细的上下文信息，这样AI给出的答案会更准确。比如让它设计测试用例，我会把接口文档、参数规则都告诉它，让它按表格格式输出。"`
    },
    
    // ===== Python测试相关 =====
    'unittest': {
        pronunciation: 'unit test',
        explain: `unittest，Python自带的测试框架，标准库。

✦ 核心特点：
• 内置：Python自带，无需安装
• 类风格：继承unittest.TestCase
• 断言丰富：assertEqual、assertTrue等
• 测试组织：setUp、tearDown

✦ 基础示例：
\`\`\`python
import unittest

class TestLogin(unittest.TestCase):
    def setUp(self):
        # 测试前准备
        print("开始测试")
    
    def test_login_success(self):
        # 测试登录成功
        result = login("admin", "123456")
        self.assertEqual(result, "success")
    
    def tearDown(self):
        # 测试后清理
        print("测试结束")

if __name__ == '__main__':
    unittest.main()
\`\`\`

✦ 与pytest对比：
• unittest：需要继承类，写法繁琐
• pytest：简洁，直接写函数即可

✦ 面试话术：
• "我了解unittest，它是Python自带的测试框架。不过日常工作中我更常用pytest，因为pytest语法更简洁，写起来更快。"`
    },
    
    'numpy': {
        pronunciation: 'nump ai',
        explain: `NumPy，Python科学计算库，处理数组和矩阵。

✦ 核心功能：
• 数组：高效的多维数组
• 数学运算：矩阵运算、统计分析
• 性能：比Python原生list快很多

✦ 测试中的应用：
• 数据处理：处理大量测试数据
• 性能测试：计算平均值、标准差
• 数据分析：分析测试结果趋势

✦ 简单示例：
\`\`\`python
import numpy as np

# 创建数组
arr = np.array([1, 2, 3, 4, 5])

# 统计分析（性能测试常用）
response_times = np.array([100, 120, 110, 130, 115])
print(f"平均响应时间: {np.mean(response_times)}ms")
print(f"最大响应时间: {np.max(response_times)}ms")
print(f"最小响应时间: {np.min(response_times)}ms")
\`\`\`

✦ 面试话术：
• "我了解NumPy的基础用法，主要用来做数据统计分析。比如性能测试后，用NumPy计算平均响应时间、最大最小值、标准差等指标。"`
    },
    
    'pandas': {
        pronunciation: 'pan das',
        explain: `pandas，Python数据分析库，处理表格数据。

✦ 核心功能：
• DataFrame：类似Excel表格
• 数据读写：读写Excel、CSV
• 数据处理：筛选、排序、统计

✦ 测试中的应用：
• 处理测试数据：从Excel读取测试用例
• 数据对比：对比数据库和接口返回数据
• 测试报告：生成测试数据报表
• 性能分析：分析性能测试结果

✦ 简单示例：
\`\`\`python
import pandas as pd

# 读取Excel测试用例
df = pd.read_excel('test_cases.xlsx')

# 筛选优先级为P0的用例
p0_cases = df[df['优先级'] == 'P0']

# 统计通过率
pass_rate = len(df[df['结果'] == '通过']) / len(df) * 100
print(f"通过率: {pass_rate}%")

# 导出结果
df.to_excel('test_result.xlsx', index=False)
\`\`\`

✦ 面试话术：
• "我用pandas处理测试数据，比如从Excel读取测试用例，或者对比数据库和接口返回的数据是否一致。pandas处理表格数据很方便，比手工对比效率高很多。"`
    }
};

// 自动标记术语的函数
function markTermsAutomatically(targetContent) {
    // 如果指定了目标内容，只处理该内容；否则处理当前激活的内容
    const contents = targetContent ? [targetContent] : document.querySelectorAll('.content.active');
    
    contents.forEach(content => {
        // 遍历所有文本节点
        const walker = document.createTreeWalker(
            content,
            NodeFilter.SHOW_TEXT,
            {
                acceptNode: function(node) {
                    // 跳过已经标记的元素和script/style标签
                    if (node.parentElement.classList.contains('term') ||
                        node.parentElement.classList.contains('explain') ||
                        node.parentElement.tagName === 'SCRIPT' ||
                        node.parentElement.tagName === 'STYLE' ||
                        node.parentElement.tagName === 'CODE' ||
                        node.parentElement.tagName === 'PRE') {
                        return NodeFilter.FILTER_REJECT;
                    }
                    return NodeFilter.FILTER_ACCEPT;
                }
            }
        );

        const nodesToProcess = [];
        while (walker.nextNode()) {
            nodesToProcess.push(walker.currentNode);
        }

        nodesToProcess.forEach(textNode => {
            let text = textNode.textContent;
            let hasMatch = false;
            
            // 检查是否包含词典中的术语
            for (const term in TERMS_DICTIONARY) {
                if (text.includes(term)) {
                    hasMatch = true;
                    break;
                }
            }
            
            if (!hasMatch) return;
            
            // 创建文档片段
            const fragment = document.createDocumentFragment();
            let lastIndex = 0;
            
            // 按照术语出现的顺序替换
            const matches = [];
            for (const term in TERMS_DICTIONARY) {
                const regex = new RegExp(term, 'g');
                let match;
                while ((match = regex.exec(text)) !== null) {
                    matches.push({
                        term: term,
                        index: match.index,
                        length: term.length
                    });
                }
            }
            
            // 按位置排序
            matches.sort((a, b) => a.index - b.index);
            
            // 去除重叠的匹配
            const validMatches = [];
            let lastEnd = -1;
            matches.forEach(match => {
                if (match.index >= lastEnd) {
                    validMatches.push(match);
                    lastEnd = match.index + match.length;
                }
            });
            
            // 构建新的HTML
            validMatches.forEach(match => {
                // 添加匹配前的文本
                if (match.index > lastIndex) {
                    fragment.appendChild(document.createTextNode(text.substring(lastIndex, match.index)));
                }
                
                // 创建术语标记
                const termData = TERMS_DICTIONARY[match.term];
                const span = document.createElement('span');
                span.className = 'term explain';
                span.setAttribute('data-pronunciation', termData.pronunciation);
                span.setAttribute('data-explain', termData.explain);
                span.textContent = match.term;
                
                const pronunIcon = document.createElement('span');
                pronunIcon.className = 'pronun-icon';
                pronunIcon.textContent = '🔊';
                span.appendChild(pronunIcon);
                
                const explainIcon = document.createElement('span');
                explainIcon.className = 'explain-icon';
                explainIcon.textContent = '❓';
                span.appendChild(explainIcon);
                
                fragment.appendChild(span);
                
                lastIndex = match.index + match.length;
            });
            
            // 添加剩余文本
            if (lastIndex < text.length) {
                fragment.appendChild(document.createTextNode(text.substring(lastIndex)));
            }
            
            // 替换原文本节点
            if (validMatches.length > 0) {
                textNode.parentNode.replaceChild(fragment, textNode);
            }
        });
    });
}

